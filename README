Calf is a library which allows "dynamic class creation" in C++, simply, one could create an instance with a given type in string, generally, this helps to make factory pattern more convenient and elegant.
